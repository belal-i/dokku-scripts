[Definition]

# Rate-limit repeated Drupal login attempts.
# This matches login submissions, not specifically failed authentication.
failregex = ^<HOST> .* "POST /user/login(?:\?.*)? HTTP/.*" 200

ignoreregex =
